# Local recognition service — Python dependencies
# On Windows with an NVIDIA GPU, install onnxruntime-gpu instead of onnxruntime
# (see recognition/install-windows.ps1 — it picks automatically).
onnxruntime>=1.20
opencv-python>=4.10
numpy>=1.26
pillow>=10.0
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
requests>=2.31
huggingface_hub>=0.34
PyYAML>=6.0
tqdm>=4.66
