# Recognition uses onnxruntime-web in the browser; no native/CUDA backend is
# needed. The real onnxruntime-node package (210 MB of native binaries pulled
# in by @huggingface/transformers) is replaced with a local stub via the
# "overrides" field in package.json — see stubs/onnxruntime-node/.
